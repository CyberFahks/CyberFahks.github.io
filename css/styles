/* ---------- Retro Portfolio CSS (1990s UI vibe) ---------- */

:root {
	--ink: #252525;
	--paper: #f5fde4;

	--bgA: #cccda4;
	--bgB: #98b2a8;
	--bgC: #b8d1ce;

	--panel: #c4d5bc;
	--panel2: #fff9f3;

	--hi: #ffffff;
	--mid: rgb(187, 198, 175);
	--lo: #b8d1ce;

	--accent: #f3f4ac;
	--accent2: #c2dbd2;

	--shadow: rgba(0, 0, 0, 0.25);
	--shadow2: rgba(0, 0, 0, 0.15);

	--radius: 10px;
	--pixel: 2px;

	--font-ui: "Franklin Gothic Sans", Tahoma, Verdana, system-ui, -apple-system,
		sans-serif;
	--font-mono: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas,
		"Courier New", monospace;
}

@media (max-width: 700px){
  .dock{
    position: static;
    margin: 16px 14px;
  }
}

* {
	box-sizing: border-box;
}
html,
body {
	height: 100%;
}
body {
	margin: 0;
	color: var(--ink);
	font-family: "Franklin Gothic Sans", "Franklin Gothic Sans Sans MT", Calibri,
		"Trebuchet MS", sans-serif;
	line-height: 1.35;
	font-size: 17px;
	background: radial-gradient(
			1200px 700px at 20% 10%,
			rgba(255, 254, 254, 0.25),
			transparent 10%
		),
		linear-gradient(180deg, var(--bgA), var(--bgB) 35%, var(--bgC));
}
.flower {
	height: 50px;
	width: 50px;
}
#main{
  padding-top: 56px;
}
/* Style the navigation menu */
/* ----- Top nav ----- */


.hero{
  min-height: calc(100vh - 56px);
  display: flex;
  flex-direction: column;
}

.hero .window-body{
  flex: 1;
}

/* push the page down so it doesn't sit under the fixed nav */
body {
	padding-top: 56px;
}
/* --- fixed top nav that does not overlap content --- */
.topnav{
  position: fixed;
  top: 0;
  left: 0;
  right: 0;

  height: 56px;
  padding: 0 12px;

  display: flex;
  align-items: center;
  justify-content: space-between;

  background: var(--panel);
  border-bottom: var(--pixel) solid #000;
  z-index: 9999;
}

/* logo link */
.topnav a.active{
  display: flex;
  align-items: center;
  height: 56px;
  padding: 0;
  background: transparent;
}

/* hamburger area */
.topnav a.icon{
  display: flex;
  align-items: center;
  justify-content: center;

  width: 56px;
  height: 56px;

  background: var(--panel);
  border-left: var(--pixel) solid #000;
}

.topnav a.icon i{ color:#000; }

/* dropdown links appear BELOW the bar */
.topnav #myLinks{
  position: absolute;
  top: 56px;
  left: 0;
  right: 0;

  background: var(--panel);
  border-bottom: var(--pixel) solid #000;

  display: none; /* your JS will toggle this */
}

.topnav #myLinks a{
  display: block;
  padding: 12px 16px;
  line-height: 1.2;
  color: #000;
}

.topnav #myLinks a:hover{
  background: rgba(255,255,255,.35);
}


/* Style the active link (or home/logo) */
.active {
	background-color: #cfe589;
	color: white;
}

/* Grain + subtle dot matrix */
body::before {
	content: "";
	position: fixed;
	inset: 0;
	pointer-events: none;
	opacity: 0.22;
	background: repeating-radial-gradient(
			circle at 1px 1px,
			rgba(0, 0, 0, 0.09) 0 1px,
			transparent 1px 4px
		),
		repeating-linear-gradient(
			0deg,
			rgba(255, 255, 255, 0.07) 0 1px,
			transparent 1px 3px
		);
	mix-blend-mode: multiply;
}

/* Skip link */
.skip-link {
	position: absolute;
	left: -999px;
	top: 14px;
	padding: 10px 14px;
	background: var(--panel);
	border: var(--pixel) solid #000;
	box-shadow: 3px 3px 0 rgba(0, 0, 0, 0.25);
	z-index: 999;
}
.skip-link:focus {
	left: 14px;
}

/* ---------- Browser bar ---------- */
.browser-bar {
	position: sticky;
	top: 0;
	z-index: 20;

	display: flex;
	align-items: center;
	justify-content: space-between;
	gap: 14px;

	padding: 10px 14px;
	background: rgba(20, 20, 20, 0.82);
	color: #eee;
	backdrop-filter: blur(6px);
	border-bottom: 1px solid rgba(255, 255, 255, 0.18);
}

.browser-left {
	display: flex;
	align-items: center;
	gap: 10px;
}
.browser-icon {
	width: 22px;
	height: 22px;
	display: grid;
	place-items: center;
	border-radius: 6px;
	background: rgba(255, 255, 255, 0.1);
}

.url-pill {
	padding: 6px 10px;
	border-radius: 999px;
	background: rgba(255, 255, 255, 0.08);
	border: 1px solid rgba(255, 255, 255, 0.18);
	font-family: "Franklin Gothic Sans", "Franklin Gothic Sans Sans MT", Calibri,
		"Trebuchet MS", sans-serif;
	font-size: 14px;
	letter-spacing: 0.2px;
}

.browser-right {
	display: flex;
	align-items: center;
	gap: 10px;
}
.tab-badge {
	width: 22px;
	height: 18px;
	border-radius: 4px;
	display: grid;
	place-items: center;
	background: rgba(255, 255, 255, 0.12);
	border: 1px solid rgba(255, 255, 255, 0.18);
	font-size: 14px;
}
.kebab {
	opacity: 0.9;
}

/* ---------- Layout ---------- */
.page {
	width: min(1100px, calc(100% - 28px));
	margin: 16px auto 24px;
}

.top-actions {
	display: flex;
	justify-content: space-between;
	gap: 14px;
	margin: 14px 0 16px;
}

.layout {
	display: grid;
	gap: 14px;
}

.columns {
	display: grid;
	gap: 14px;
	grid-template-columns: 1fr;
}
@media (min-width: 920px) {
	.columns {
		grid-template-columns: 1fr 1.15fr;
	}
}

/* ---------- Window styling ---------- */
.window {
	border-radius: var(--radius);
	background: var(--panel);
	border: var(--pixel) solid #000;
	box-shadow: 6px 6px 0 rgba(0, 0, 0, 0.18);
	overflow: hidden;
}

.window-titlebar {
	display: flex;
	align-items: center;
	gap: 8px;

	padding: 8px 10px;
	border-bottom: var(--pixel) solid #000;
	background: linear-gradient(
			180deg,
			rgba(255, 255, 255, 0.66),
			rgba(255, 255, 255, 0.18)
		),
		linear-gradient(
			90deg,
			rgba(190, 240, 227, 0.85),
			rgba(196, 196, 255, 0.55)
		);
}

.window-title {
	font-family: "Franklin Gothic Sans Sans", "Franklin Gothic Sans Sans MT",
		Calibri, "Trebuchet MS", sans-serif;
	letter-spacing: 0.8px;
	font-size: 15px;
}

.title-spacer {
	flex: 1;
}
.tiny {
	font-family: "Franklin Gothic Sans Sans", "Franklin Gothic Sans Sans MT",
		Calibri, "Trebuchet MS", sans-serif;
	font-size: 14px;
	opacity: 0.85;
}

.window-body {
	padding: 14px;
	background: linear-gradient(180deg, var(--panel), var(--panel2));
}

/* Window buttons */
.win-btn {
	width: 26px;
	height: 22px;
	display: grid;
	place-items: center;
	cursor: pointer;

	background: #f0e7de;
	border: var(--pixel) solid #000;
	border-radius: 6px;

	box-shadow: inset 2px 2px 0 var(--hi), inset -2px -2px 0 var(--lo),
		2px 2px 0 rgba(0, 0, 0, 0.15);
}
.win-btn:active {
	transform: translate(1px, 1px);
	box-shadow: inset -2px -2px 0 var(--hi), inset 2px 2px 0 var(--lo),
		1px 1px 0 rgba(0, 0, 0, 0.12);
}
.chip {
	width: 22px;
	height: 22px;
	display: grid;
	place-items: center;
	border: var(--pixel) solid #000;
	border-radius: 6px;
	background: rgba(255, 255, 255, 0.55);
}

/* ---------- Buttons ---------- */
.btn {
	display: inline-flex;
	align-items: center;
	justify-content: center;
	gap: 8px;

	text-decoration: none;
	color: var(--ink);
	user-select: none;
	cursor: pointer;

	padding: 10px 14px;
	background: #f2e7df;
	border: var(--pixel) solid #000;
	border-radius: 10px;

	box-shadow: inset 2px 2px 0 var(--hi), inset -2px -2px 0 var(--lo),
		3px 3px 0 var(--shadow2);
}

.btn.big {
	min-width: 170px;
	font-family: "Franklin Gothic Sans Sans", "Franklin Gothic Sans Sans MT",
		Calibri, "Trebuchet MS", sans-serif;
	letter-spacing: 0.6px;
}

.btn.small {
	padding: 8px 10px;
	border-radius: 9px;
	font-size: 14px;
}

.btn.play {
	background: var(--accent);
}

.btn:active {
	box-shadow: inset -2px -2px 0 var(--hi), inset 2px 2px 0 var(--lo),
		2px 2px 0 rgba(0, 0, 0, 0.12);
}

.btn:focus-visible,
.icon-btn:focus-visible,
.dock-item:focus-visible,
.select:focus-visible,
.speaker:focus-visible,
.contact-link:focus-visible {
	outline: 3px solid rgba(0, 0, 0, 0.55);
	outline-offset: 2px;
}

/* ---------- Hero player ---------- */
.player-grid {
	display: grid;
	gap: 14px;
	align-items: start;
	grid-template-columns: 1fr;
}
@media (min-width: 860px) {
	.player-grid {
		grid-template-columns: 1.1fr 1fr;
	}
}

.screen {
	position: relative;
	border-radius: 14px;
	border: var(--pixel) solid #000;
	overflow: hidden;
	background: linear-gradient(
			90deg,
			#ffd3a8,
			#b7b0ff 28%,
			#b7e6d6 58%,
			#fff2a7 72%,
			#a9a9a9
		),
		repeating-linear-gradient(
			0deg,
			rgba(0, 0, 0, 0.1) 0 1px,
			transparent 1px 3px
		);
	min-height: 280px;
	box-shadow: inset 0 0 0 3px rgba(255, 255, 255, 0.35);
}

/* scanlines + dot mask */
.screen::before {
	content: "";
	position: absolute;
	inset: 0;
	pointer-events: none;
	opacity: 0.35;
	background: repeating-linear-gradient(
			0deg,
			rgba(0, 0, 0, 0.25) 0 1px,
			transparent 1px 4px
		),
		repeating-radial-gradient(
			circle at 1px 1px,
			rgba(0, 0, 0, 0.22) 0 1px,
			transparent 1px 4px
		);
	mix-blend-mode: multiply;
}

/* side “dither stripe” */
.screen::after {
	content: "";
	position: absolute;
	left: 0;
	top: 0;
	bottom: 0;
	width: 22%;
	background: linear-gradient(
			180deg,
			rgba(255, 255, 255, 0.3),
			rgba(0, 0, 0, 0.08)
		),
		repeating-linear-gradient(
			90deg,
			rgba(0, 0, 0, 0.2) 0 2px,
			transparent 2px 5px
		);
	opacity: 0.6;
}

.screen-badge {
	position: absolute;
	top: 10px;
	left: 10px;
	display: flex;
	align-items: center;
	gap: 6px;
	z-index: 2;
	font-family: "Franklin Gothic Sans Sans", "Franklin Gothic Sans Sans MT",
		Calibri, "Trebuchet MS", sans-serif;
	font-size: 14px;
}
.badge-text {
	padding: 3px 8px;
	border: var(--pixel) solid #000;
	border-radius: 8px;
	background: rgba(255, 255, 255, 0.65);
}
.badge-heart {
	padding: 2px 8px;
	border: var(--pixel) solid #000;
	border-radius: 10px;
	background: rgba(255, 255, 255, 0.65);
}

.screen-center {
	position: relative;
	z-index: 1;
	height: 100%;
	min-height: 280px;
	display: grid;
	place-items: center;
}

.screen-avatar {
	width: 140px;
	height: 140px;
	border-radius: 999px;
	border: var(--pixel) solid #000;
	background: radial-gradient(
			circle at 35% 30%,
			rgba(255, 255, 255, 0.6),
			transparent 55%
		),
		radial-gradient(circle at 70% 80%, rgba(0, 0, 0, 0.2), transparent 60%),
		linear-gradient(180deg, rgba(255, 255, 255, 0.25), rgba(0, 0, 0, 0.08));
	box-shadow: 5px 5px 0 rgba(0, 0, 0, 0.2);
}

.screen-caption {
	position: absolute;
	left: 10px;
	right: 10px;
	bottom: 10px;
	display: flex;
	justify-content: space-between;
	gap: 10px;
	z-index: 2;
	font-family: var(--font-mono);
	font-size: 14px;
}
.filepill {
	padding: 4px 8px;
	border: var(--pixel) solid #000;
	border-radius: 9px;
	background: rgba(0, 0, 0, 0.55);
	color: #fff;
	white-space: nowrap;
	overflow: hidden;
	text-overflow: ellipsis;
}
.dim {
	padding: 4px 8px;
	border: var(--pixel) solid #000;
	border-radius: 9px;
	background: rgba(255, 255, 255, 0.65);
}

/* Track card */
.track {
	border-radius: 14px;
	border: var(--pixel) solid #000;
	background: rgba(255, 255, 255, 0.55);
	box-shadow: inset 2px 2px 0 rgba(255, 255, 255, 0.7);
	padding: 14px;
}

.track-row {
	display: flex;
	align-items: flex-start;
	justify-content: space-between;
	gap: 10px;
}
.track-title {
	font-weight: 800;
	font-size: 18px;
}
.track-artist {
	font-family: var(--font-mono);
	font-size: 14px;
	opacity: 0.85;
}
.track-actions {
	display: flex;
	gap: 8px;
}

.icon-btn {
	width: 36px;
	height: 32px;
	display: grid;
	place-items: center;
	border-radius: 10px;
	background: #c9e0ab;
	border: var(--pixel) solid #000;
	box-shadow: inset 2px 2px 0 var(--hi), inset -2px -2px 0 var(--lo),
		2px 2px 0 rgba(0, 0, 0, 0.14);
	cursor: pointer;
}

.track-rule {
	height: 2px;
	background: rgba(0, 0, 0, 0.65);
	margin: 10px 0;
}

.controls {
	display: flex;
	align-items: center;
	gap: 8px;
	flex-wrap: wrap;
	margin-bottom: 10px;
}

.channel-row {
	display: flex;
	align-items: center;
	gap: 10px;
	flex-wrap: wrap;
}

.label {
	font-family: var(--font-mono);
	font-size: 14px;
}
.select {
	min-width: min(420px, 100%);
	padding: 10px 14px;
	border-radius: 10px;
	border: var(--pixel) solid #000;
	background: #f7f0ea;
	box-shadow: inset 2px 2px 0 var(--hi), inset -2px -2px 0 var(--lo);
}

.speaker {
	width: 50px;
	height: 42px;
	border-radius: 14px;
	border: var(--pixel) solid #000;
	background: #f2e7df;
	cursor: pointer;
	box-shadow: inset 2px 2px 0 var(--hi), inset -2px -2px 0 var(--lo),
		2px 2px 0 rgba(0, 0, 0, 0.14);
}

/* ---------- About ---------- */
.about {
	display: grid;
	gap: 14px;
	grid-template-columns: 1fr;
}
@media (min-width: 640px) {
	.about {
		grid-template-columns: 170px 1fr;
	}
}

.portrait {
	height: 170px;
	border-radius: 14px;
	border: var(--pixel) solid #000;
	background: linear-gradient(
			140deg,
			rgba(255, 255, 255, 0.65),
			rgba(0, 0, 0, 0.06)
		),
		repeating-linear-gradient(
			45deg,
			rgba(0, 0, 0, 0.1) 0 2px,
			transparent 2px 7px
		);
	box-shadow: 5px 5px 0 rgba(0, 0, 0, 0.18);
}

.lede {
	margin: 0 0 10px;
	font-size: 16px;
}

.bullets {
	margin: 10px 0 0;
	padding-left: 18px;
}
.tags {
	display: flex;
	flex-wrap: wrap;
	gap: 8px;
	margin-top: 10px;
}
.tag {
	padding: 6px 10px;
	border-radius: 999px;
	border: var(--pixel) solid #000;
	background: rgba(184, 230, 214, 0.75);
	font-family: var(--font-mono);
	font-size: 14px;
}

/* ---------- Projects ---------- */
.project-list {
	display: grid;
	gap: 14px;
}
.project {
	padding: 14px;
	border-radius: 14px;
	border: var(--pixel) solid #000;
	background: rgba(255, 255, 255, 0.62);
	box-shadow: inset 2px 2px 0 rgba(255, 255, 255, 0.7);
}

.project-head {
	display: flex;
	justify-content: space-between;
	gap: 10px;
	flex-wrap: wrap;
}
.project-name {
	font-weight: 900;
	letter-spacing: 0.2px;
}
.project-meta {
	font-family: var(--font-mono);
	font-size: 14px;
	opacity: 0.85;
}
.project-desc {
	margin: 8px 0 10px;
}
.project-actions {
	display: flex;
	gap: 8px;
	flex-wrap: wrap;
}

.divider {
	height: 10px;
	border: var(--pixel) solid #000;
	border-radius: 10px;
	background: repeating-linear-gradient(
		90deg,
		rgba(0, 0, 0, 0.18) 0 4px,
		transparent 4px 10px
	);
	margin: 14px 0;
	opacity: 0.45;
}

.mini-grid {
	display: grid;
	gap: 10px;
	grid-template-columns: 1fr;
}
@media (min-width: 700px) {
	.mini-grid {
		grid-template-columns: repeat(3, 1fr);
	}
}
.stat {
	border-radius: 14px;
	border: var(--pixel) solid #000;
	background: rgba(255, 255, 255, 0.58);
	box-shadow: inset 2px 2px 0 rgba(255, 255, 255, 0.7);
	overflow: hidden;
}
.stat-top {
	padding: 8px 10px;
	background: linear-gradient(
		90deg,
		rgba(216, 102, 153, 0.65),
		rgba(212, 230, 184, 0.7)
	);
	border-bottom: var(--pixel) solid #000;
	font-family: "Franklin Gothic Medium", "Arial Narrow", Arial, sans-serif;
	font-size: 14px;
	letter-spacing: 0.6px;
}
.stat-bottom {
	padding: 10px;
	font-weight: 700;
}

/* ---------- Mixtapes ---------- */
.mixtapes {
	display: grid;
	gap: 14px;
	grid-template-columns: 1fr;
}
@media (min-width: 860px) {
	.mixtapes {
		grid-template-columns: 1fr 1fr;
	}
}

.tape {
	border-radius: 14px;
	border: var(--pixel) solid #000;
	background: rgba(255, 255, 255, 0.58);
	box-shadow: inset 2px 2px 0 rgba(255, 255, 255, 0.7);
	padding: 14px;
}
.tape-label {
	display: inline-block;
	padding: 6px 10px;
	border-radius: 999px;
	border: var(--pixel) solid #000;
	background: rgba(0, 0, 0, 0.65);
	color: #fff;
	font-family: var(--font-mono);
	font-size: 14px;
	margin-bottom: 8px;
}
.tape-list {
	margin: 8px 0 0;
	padding-left: 20px;
}

/* ---------- Contact ---------- */
.contact-grid {
	display: grid;
	gap: 10px;
	grid-template-columns: 1fr;
	margin: 14px 0 6px;
}
@media (min-width: 760px) {
	.contact-grid {
		grid-template-columns: repeat(3, 1fr);
	}
}

.contact-card {
	border-radius: 14px;
	border: var(--pixel) solid #000;
	background: rgba(255, 255, 255, 0.6);
	box-shadow: inset 2px 2px 0 rgba(255, 255, 255, 0.7);
	padding: 14px;
}
.contact-label {
	font-family: var(--font-mono);
	font-size: 14px;
	opacity: 0.9;
	margin-bottom: 6px;
}
.contact-link {
	font-weight: 800;
	color: #000;
	text-decoration: underline;
	text-decoration-thickness: 2px;
}
.tiny-note {
	font-family: var(--font-mono);
	font-size: 14px;
	opacity: 0.85;
	margin: 10px 0 0;
}

/* ---------- Dock nav ---------- */
.dock {
	position: sticky;
	bottom: 10px;
	margin: 16px 0 8px;

	display: flex;
	gap: 20px;
	justify-content: space-between;
	padding: 20px;

	border-radius: 14px;
	border: var(--pixel) solid #000;
	background: rgba(243, 236, 229, 0.78);
	box-shadow: 6px 6px 0 rgba(0, 0, 0, 0.16);
	backdrop-filter: blur(6px);
}

.dock-item {
	flex: 1;
	display: flex;
	align-items: center;
	justify-content: center;
	gap: 8px;

	text-decoration: none;
	color: #000;

	border-radius: 14px;
	border: var(--pixel) solid #000;
	background: #f2e7df;

	padding: 10px 10px;
	box-shadow: inset 2px 2px 0 var(--hi), inset -2px -2px 0 var(--lo),
		2px 2px 0 rgba(0, 0, 0, 0.14);
}

.dock-item:hover {
	background: linear-gradient(180deg, rgba(184, 230, 214, 0.85), #f2e7df);
}

.dock-icon {
	font-size: 18px;
}
.dock-text {
	font-family: var(--font-mono);
	font-size: 14px;
	letter-spacing: 0.4px;
}

@media (max-width: 700px){
  .dock{
    position: static;
    margin: 16px 14px;
  }
}

@media (max-width: 560px) {
	.dock-text {
		display: none;
	}
}

/* ---------- Footer ---------- */
.footer {
	display: flex;
	justify-content: center;
	gap: 10px;
	margin-top: 10px;
	padding: 10px 0 0;
}

.tiny {
	font-family: var(--font-mono);
	font-size: 14px;
	opacity: 0.85;
}

